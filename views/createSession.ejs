<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Create Session</title>
  <link href="/styles/output.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
  <%- include ('partials/header') %>
  <main class="container mx-auto max-w-md py-10">
    <h2 class="text-2xl font-bold mb-6 text-center">Create a New Session</h2>
    <form action="/session/new" method="POST" class="bg-white p-6 shadow-md rounded">
      <div class="mb-4">
        <label for="sportId" class="block text-sm font-bold mb-2">Select Sport</label>
        <% if (sports.length === 0) { %>
          <p>No sports available. Please add a sport first.</p>
        <% } else { %>
          <select name="sportId" id="sportId" class="w-full p-2 border rounded">
            <% sports.forEach(sport => { %>
              <option value="<%= sport.id %>"><%= sport.name %></option>
            <% }); %>
          </select>
        <% } %>
      </div>
      <div class="mb-4">
        <label for="date" class="block text-sm font-bold mb-2">Date</label>
        <input type="date" name="date" id="date" class="w-full p-2 border rounded" required>
      </div>
      <div class="mb-4">
        <label for="venue" class="block text-sm font-bold mb-2">Venue</label>
        <input type="text" name="venue" id="venue" class="w-full p-2 border rounded" required>
      </div>
      <div class="mb-6">
        <label for="additionalPlayers" class="block text-sm font-bold mb-2">Additional Players Needed</label>
        <input type="number" name="additionalPlayers" id="additionalPlayers" class="w-full p-2 border rounded" required>
      </div>
      <button type="submit" class="bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700">Create Session</button>
    </form>
  </main>
  <%- include ('partials/footer') %>
</body>
</html>
